# RPC endpoint for the chain where Permit2 lives (e.g. Infura or Alchemy)
RPC_URL=https://mainnet.infura.io/v3/YOUR_KEY

# Private key of the spender (this backend account will pay gas and receive funds)
SPENDER_PRIVATE_KEY=0x...

# Firebase Admin: either set a JSON string or use GOOGLE_APPLICATION_CREDENTIALS
# Example: export FIREBASE_SERVICE_ACCOUNT_JSON='{"type":...}'
FIREBASE_SERVICE_ACCOUNT_JSON=
FIREBASE_DATABASE_URL=https://tlord-1ab38-default-rtdb.firebaseio.com

# (Optional) Permit2 address override
PERMIT2_ADDRESS=
